#!/bin/bash
# Soplos System Services launcher wrapper

export WMCLASS="org.soplos.systemservice"

INSTALL_DIR="/usr/share/soplos-system-services"
if [ -d "$INSTALL_DIR" ]; then
    cd "$INSTALL_DIR"
else
    cd "$(dirname "$(readlink -f "$0")")"
fi

exec python3 main.py "$@"
